    // formData.append('type', JSON.stringify(state.type));
    // formData.append('title', JSON.stringify(state.type));
    // formData.append('dateYear',  JSON.stringify(state.type));
    // formData.append('description', JSON.stringify(state.type));
    // formData.append('category', JSON.stringify(state.type));
    // formData.append('actors',  JSON.stringify(state.type));
    // formData.append('writer',  JSON.stringify(state.type));
    // formData.append('director', JSON.stringify(state.type));
    // formData.append('trailer', JSON.stringify(state.type));
    // formData.append('image', image);
    // formData.append('video', video);

    // const [image, setImage] = useState(null);
    // const handleImageUpload = (event) => {
    //     setImage(event.target.files[0]);

    //     console.log(event.target.files[0])
    // };
    // const [video, setVideo] = useState(null);
    // const handleVideoUpload = (event) => {
    //     setVideo(event.target.files[0]);

    //     console.log(event.target.files[0])
    // };
